Q35. Write a PHP script using AJAX concept, to check user name and password are valid or Invalid
(use database to store user name and password)

Answer::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::

<html>
<head>
<script>
function checkUser()
{
var u = document.getElementById("t1").value;
var p = document.getElementById("t2").value;
var x = new XMLHttpRequest();x.open("GET", "check.php?user=" + u + "&pass=" + p, true);
x.send();
x.onreadystatechange = function()
{
if(x.readyState == 4 && x.status == 200)
{
document.getElementById("res").innerHTML = x.responseText;
}
}
}
</script>
</head>
<body>
Username: <input type="text" id="t1"><br><br>
Password: <input type="password" id="t2"><br><br>
<input type="button" value="Login" onclick="checkUser()">
<h3 id="res"></h3>
</body>
</html>
<?php
$user = $_GET['user'];
$pass = $_GET['pass'];
$con = pg_connect("host=localhost port=5432 dbname=ty user=postgres password=tybcs");if(!$con)
{
echo "Connection failed";
}
$q = "select * from users where username='$user' and password='$pass'";
$res = pg_query($con, $q);
if(pg_num_rows($res) > 0)
{
echo "Valid User";
}
else
{
echo "Invalid User";
}
?>
