Q2.Write a PHP script to keep track of number of times webpages has been access[Use Cookie].

<?php
	if(isset($_COOKIE["count"]))
	{
		$count=$_COOKIE["count"]+1;
	}
	else
	{
		$count=1;
	}
	setcookie("count","$count",time()+3600);
	echo "Count is :: ".$count;
?>
